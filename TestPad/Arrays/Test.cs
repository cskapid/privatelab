namespace TestPad.Arrays
{
    //public class ArraysAndProblems
    //{
    //    static int FindMajorityElement(int[] intArray)
    //    {

    //    }
        /*
        static void ZeroOffMatrix()
        {
            {
                //int[,,] matrix = new int[4,4,4];
                //Console.WriteLine(matrix.Rank);
                ////Console.WriteLine(matrix.GetUpperBound(0));
            }
            {
                int[][] jmatrix = new int[][] { new int[] { 0, 1, 1, 0 }
                , new int[] { 1, 1, 0, 1 }
                , new int[] { 1, 1, 1, 1 }
                , new int[] { 1, 1, 1, 1 } };

                int[] zeros = new int[16];
                {
                    int k = 0;
                    for (int i = 0; i < jmatrix.Length; i++)
                    {
                        for (int j = 0; j < jmatrix[i].Length; j++)
                        {
                            if (jmatrix[i][j] == 0)
                            {
                                zeros[k++] = i * jmatrix.Length + j + 1;
                            }
                        }
                    }
                }

                foreach (var item in zeros)
                {
                    if (item == 0)
                        break;

                    var i = (item - 1) / jmatrix.Length;
                    var j = (item - 1) % jmatrix.Length;
                    Console.WriteLine(i + "," + j);
                }
            }
        }
        */
    //}
}
